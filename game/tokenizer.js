/*
 * tokenizer.js
 * Takes in a UserInput object and processes the string into tokens.
 * Tokens may be replaced by synonyms.
 */

var utils = require('./UserInput.js')

class Tokenizer {
    constructor() {
    }

    tokenize(userInputObj) {
        return userInputObj
    }
}

module.exports = Tokenizer